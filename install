#installation instructions
run the compile.sh script to automatically copy and compile the US english language rules and phonemes. Please make sure you have the latest version of espeak or espeak ng. Consult your linux distribution's package repository. If your linux distribution doesn't have the latest version, build from source.

if sudo privilages are needed, the script will prompt you for a password. If either the directory /usr/share/espeak-data 
is missing or is not a directory, the script will exit with an error.

